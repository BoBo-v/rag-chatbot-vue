# Vue 3 + TypeScript + Vite

# 目录
### src/
### ├── components/     # UI组件
### ├── composables/     # 页面逻辑解耦
### ├── views/          # 页面
### ├── stores/         # 状态管理（后面做对话）
### ├── services/       # API请求（Ollama）
### ├── styles/         # css样式
### ├── types/          # TS类型
### ├── utils/          # 工具函数（节流 / 调度）

#### 初期调用为本地通过ollama调用本地模型