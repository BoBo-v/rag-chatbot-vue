# 多模型对比功能目标计划

## 1. 功能定位

首版目标是做“多模型对比”，不是做“多 Agent 系统”。

功能定义：

- 用户输入一个问题。
- 选择 2 个或多个模型并发回答。
- 页面并排展示每个模型的回答、状态、耗时、错误信息。
- 用户可以把成功回答打包发送给指定模型，让它生成最终汇总答案。
- 后续可以保存对比记录，并从历史中重新打开。

首版明确不做：

- Agent 角色编排。
- Agent 工具调用。
- Agent 之间互相通信。
- 自动任务拆解。
- Git 冲突处理式的“接受 A / 接受 B / 手动编辑片段”。
- 自动判定某个模型绝对正确。

可以理解为：

```text
用户问题
  -> 模型 A 回答
  -> 模型 B 回答
  -> 模型 C 回答
  -> 汇总模型整合成功结果
  -> 最终答案
```

## 2. 当前项目校验结论

当前项目不需要推翻重做，但需要局部架构升级。

已确认现状：

- `src/services/stream.ts` 根据全局 `settings.provider` 分发到当前 provider。
- `src/services/providers/*.ts` 里的 provider 适配器直接读取全局 `settings`。
- `src/composables/useChatView.ts` 是单模型聊天流程：一个用户消息对应一个 assistant 流式回答。
- `src/composables/useTypewriterStream.ts` 当前队列按单个消息流工作，不适合多个模型同时输出。
- `src/db/index.ts` 目前只有普通会话 `conversations/messages`，没有 comparison session/run 表。
- `src/views/StyleChat.vue` 是单一聊天主界面，建议继续保留普通聊天，不把对比流程强塞进去。

结论：

- 普通聊天继续保留。
- 新增一条独立“对比模式”链路。
- 模型调用必须从“读取全局 settings”改成“传入运行时模型配置快照”。
- 对比模式需要独立状态管理、独立 UI、独立持久化结构。

## 3. 生产环境主要风险

### 3.1 成本放大

多模型对比会天然放大请求成本。

可能出现：

- 2 个模型回答就是至少 2 次模型调用。
- 再做一次汇总就是第 3 次模型调用。
- 带文件时，每个模型都会重复接收文件内容，token 成本线性上升。
- 带图片时，视觉模型成本和延迟更高。
- 用户连续重试某个模型，会继续增加成本。

首版控制策略：

- 默认最多选择 2 个模型。
- 可配置上限，建议最大 3 个。
- 汇总前提示“将把成功回答发送给指定模型进行汇总”。
- 对文件内容、长上下文做截断或 token 预算提示。
- 每个 run 记录 `latencyMs`，后续可扩展 token/cost 统计。

目标指标：

- 首版单次对比最多 2 个模型。
- 单次对比最多 1 个汇总 run。
- 文件内容超过预算时必须提示或截断。

### 3.2 延迟和部分完成

多个模型速度不同，不能让慢模型拖垮整个体验。

可能出现：

- A 模型 5 秒完成，B 模型 60 秒还在输出。
- 一个模型请求超时，另一个模型成功。
- 用户想停止某一个模型，而不是停止全部。
- 汇总时有模型还没完成。

首版控制策略：

- 每个模型 run 独立状态。
- 允许部分成功。
- 汇总默认只使用 `done` 的结果。
- 提供“停止单个模型”和“停止全部”。
- 某个模型失败后可单独重试。

目标指标：

- 一个 run 失败不影响其他 run。
- 一个 run 被停止不影响其他 run。
- 汇总按钮只在至少一个 run 成功后可用。

### 3.3 配置污染

这是当前项目做多模型对比的第一风险。

当前 provider 调用直接读取全局 `settings`。多模型并发时，如果不改，可能出现：

- 用户选择 A 模型和 B 模型，但两个请求实际读到同一个全局 model。
- 请求过程中用户打开设置并修改 provider，未完成请求读到新配置。
- API key/baseURL/model 混用。
- UI 显示为 qwen，实际请求发给了 OpenAI 或其他兼容接口。

首版控制策略：

- 请求开始时生成 `ModelRuntimeConfig` 快照。
- provider 请求全过程只读 runtime，不再读全局 `settings`。
- 普通聊天也通过 `createRuntimeFromSettings(settings)` 生成 runtime，以保持架构统一。

目标指标：

- 每个 run 保存 runtime 快照。
- 每个 run 展示 provider/model。
- 对比模式请求过程中修改全局设置，不影响已启动 run。

### 3.4 答案正确性风险

多个模型都回答，不代表答案更正确。

可能出现：

- 多个模型一起幻觉。
- 某个模型表达更自信，但事实错误。
- 汇总模型偏向采纳更长、更完整但不一定正确的回答。
- 两个模型给出冲突结论，汇总模型强行合并。
- 多数模型一致，但少数模型反而是对的。

首版控制策略：

- 汇总 prompt 不要求“投票选赢家”。
- 汇总输出要包含：
  - 一致结论
  - 分歧点
  - 不确定点
  - 最终建议
- UI 不显示“胜者模型”，只显示“汇总答案”。
- 对高风险问题保留“请核验”的提示空间。

目标指标：

- 汇总 prompt 明确禁止盲目投票。
- 存在冲突时要求列出冲突点。
- 不把“模型多数一致”当成正确性证明。

### 3.5 可观测性不足

多模型对比出问题时，如果没有记录，很难复盘。

需要记录：

- session id
- run id
- provider/model
- runtime 快照
- prompt 快照
- 请求开始时间
- 请求结束时间
- latencyMs
- 状态：done/error/aborted
- errorMessage
- 是否被用户停止
- 汇总时使用了哪些 run
- 汇总 prompt 版本

首版控制策略：

- 内存版也要先设计这些字段。
- 持久化阶段直接落库。
- 控制台日志只作为开发调试，不作为主要记录方式。

目标指标：

- 每个 run 至少记录状态、模型、耗时、错误。
- 汇总 run 记录引用的 source run ids。

### 3.6 附件和视觉模型能力

多模型对比时，附件会带来模型能力不一致问题。

图片场景：

- 有些模型不支持图片。
- Ollama 本地模型是否支持图片目前只能粗略用模型名判断。
- OpenAI/Claude 视觉格式不同。

文件场景：

- 文件内容会重复发送给多个模型。
- 文件越大，成本越高。
- 汇总时会再次发送各模型回答，成本继续上升。

首版控制策略：

- 如果问题带图片，只允许选择支持视觉的模型。
- 不支持视觉的模型显示 warning 或禁用。
- 文件内容进入对比前做大小限制。
- 汇总时只发送模型回答，不重复发送原始大文件，除非后续明确需要。

目标指标：

- 图片输入时，非视觉模型不可直接发起对比。
- 文件输入沿用当前大小限制，并在多模型模式下提示成本。

### 3.7 安全和隐私

首版不引入 Agent 工具调用，安全边界相对可控，但仍有风险。

可能出现：

- 用户上传文件里包含敏感信息，多模型对比会把它发送给多个 provider。
- 汇总会把多个模型回答再次发送给汇总 provider。
- 文件内容里可能包含 prompt injection，诱导模型泄露或忽略规则。
- 将来如果引入工具调用，风险会显著上升。

首版控制策略：

- 汇总前提示会发送哪些回答给哪个模型。
- 对外部 provider 明确标记“将发送到远程服务”。
- 不引入任何高权限工具调用。
- 不让模型执行本地命令、网络请求或文件操作。

目标指标：

- 首版无 Agent 工具调用。
- 首版无本地命令执行。
- 首版无自动外部检索。
- 汇总前用户能看到目标汇总模型。

### 3.8 状态恢复和持久化

如果只做内存版，刷新页面会丢失对比结果。

首版可以接受内存版，但需要明确：

- 页面刷新不保证恢复未保存对比。
- 已完成结果后续通过持久化阶段保存。
- 普通聊天历史不受影响。

持久化阶段需要注意：

- workflowVersion
- promptVersion
- runtimeSnapshot
- run 状态
- summaryRun 引用哪些 source runs

目标指标：

- 阶段 2 内存版允许刷新丢失。
- 阶段 4 后刷新页面可重新打开已完成对比记录。

## 4. 核心数据模型

建议新增类型：

```ts
export interface ModelRuntimeConfig {
    provider: 'ollama' | 'openai' | 'claude'
    label: string
    model: string
    baseUrl?: string
    apiKey?: string
    systemPrompt: string
    maxContextTokens: number
}

export type ComparisonRunStatus =
    | 'idle'
    | 'loading'
    | 'streaming'
    | 'done'
    | 'error'
    | 'aborted'

export interface ComparisonRun {
    id: string
    sessionId: string
    config: ModelRuntimeConfig
    content: string
    status: ComparisonRunStatus
    errorMessage?: string
    startedAt?: number
    finishedAt?: number
    latencyMs?: number
    sourceRunIds?: string[]
}

export interface ComparisonSession {
    id: string
    conversationId?: number
    prompt: string
    images?: ImageAttachment[]
    files?: FileAttachment[]
    runs: ComparisonRun[]
    summaryRun?: ComparisonRun
    workflowVersion: number
    promptVersion: number
    createdAt: number
    updatedAt: number
}
```

## 5. 数据库计划

建议 Dexie 升到 version 4，新增表：

```ts
comparisonSessions: 'id, conversationId, updatedAt, createdAt'
comparisonRuns: 'id, sessionId, status, startedAt, finishedAt'
```

首版内存版可以先不落库，但类型和字段要按持久化目标设计。

不建议首版把普通 `messages` 表改成 `groupId/variantId`：

- 普通聊天和对比模式行为差异大。
- 独立 comparison 表能降低对现有聊天、搜索、继续生成、重试逻辑的影响。
- 后续如果需要把对比结果插入普通会话，可以做“导出到会话”功能。

## 6. 服务层计划

### 6.1 增加运行时配置

新增 `src/types/model.ts` 或 `src/services/runtime.ts`：

- `ModelRuntimeConfig`
- `createRuntimeFromSettings(settings)`
- `validateRuntimeConfig(config)`
- `getRuntimeDisplayName(config)`

### 6.2 参数化 provider 调用

当前：

```ts
generateStreamWithContext(messages, userText, onChunk, onDone, signal)
```

目标：

```ts
generateStreamWithContext({
    messages,
    userText,
    runtime,
    onChunk,
    onDone,
    signal,
})
```

provider 适配器目标：

```ts
ollamaStream(messages, userText, runtime, onChunk, onDone, signal)
openaiStream(messages, userText, runtime, onChunk, onDone, signal)
claudeStream(messages, userText, runtime, onChunk, onDone, signal)
```

普通聊天仍然从当前 `settings` 生成 runtime，行为保持不变。

### 6.3 对比状态管理

新增 `src/composables/useModelComparison.ts`：

- `selectedRuntimes`
- `comparisonPrompt`
- `runs`
- `summaryRun`
- `startComparison()`
- `stopRun(runId)`
- `stopAll()`
- `retryRun(runId)`
- `summarizeWith(runtime)`

每个 run 独立拥有：

- `AbortController`
- status
- content
- errorMessage
- startedAt/finishedAt/latencyMs
- runtime snapshot

不要复用当前单消息 `streamCtrl`。

## 7. UI 计划

首版建议新增独立对比界面，不破坏普通聊天主界面。

入口方案：

- 顶部增加“普通 / 对比”切换。
- 或输入区增加“对比模式”开关。

新增组件建议：

- `src/views/CompareChat.vue`
- `src/components/ComparisonPanel.vue`
- `src/components/ComparisonRunCard.vue`
- `src/components/ComparisonSummary.vue`
- `src/components/ModelRuntimePicker.vue`

首版 UI：

- 顶部：问题、所选模型、停止全部、重新运行。
- 中间：多列模型回答。
- 每列显示 provider、model、状态、耗时、错误、单独重试、单独停止。
- 底部或右侧：选择汇总模型，生成最终答案。
- 汇总区显示使用了哪些模型结果。

不做：

- 接受 A / 接受 B。
- 手动合并片段。
- 类 Git 冲突编辑器。
- 自动评分排行榜。

## 8. 汇总功能计划

汇总请求输入包括：

- 原始问题。
- 每个成功模型的 provider/model 名称。
- 每个成功模型的完整回答。
- 失败模型只记录失败原因，不作为回答正文。

建议首版汇总 prompt：

```text
你将看到同一个问题下多个模型的回答。
请综合它们的信息，输出一个最终答案。

要求：
1. 不要盲目投票，优先选择事实更完整、逻辑更严谨的内容。
2. 如果多个回答冲突，请指出冲突点。
3. 如果信息不足，请明确说明不确定之处。
4. 不要声称某个模型一定正确。
5. 最后给出可直接使用的最终回答。
```

首版使用 Markdown 输出，暂不强制 JSON，避免解析失败影响体验。

后续可升级为结构化输出：

```json
{
  "summary": "...",
  "agreements": [],
  "conflicts": [],
  "uncertainties": [],
  "finalAnswer": "..."
}
```

## 9. 实施阶段

### 阶段 1：模型运行时参数化

目标：

- 普通聊天行为不变。
- `generateStreamWithContext` 支持传入 runtime。
- provider 适配器不再强依赖全局 `settings`。

改动范围：

- `src/types/model.ts`
- `src/services/runtime.ts`
- `src/services/stream.ts`
- `src/services/providers/*.ts`
- `src/composables/useChatView.ts`

验收：

- 普通聊天 Ollama 可用。
- 普通聊天 OpenAI 兼容接口可用。
- 普通聊天 Claude 可用。
- 修改设置不影响已启动请求。
- `npm run lint` 通过。
- `npm run build` 通过。

完成记录：

- 已完成 `ModelRuntimeConfig` 类型：`src/types/model.ts`。
- 已完成 `createRuntimeFromSettings(settings)`：`src/services/runtime.ts`。
- 已将 `generateStreamWithContext` 改为传入 runtime 的对象参数。
- 已将 Ollama/OpenAI/Claude provider stream 请求改为使用 runtime，不再直接读取全局 `settings`。
- 已将普通聊天 `useChatView.ts` 改为请求开始时生成 runtime 快照并传入。
- 已验证 `npm run lint` 通过。
- 已验证 `npm run build` 通过。
- 已提交：`debc651 重构：参数化模型运行时配置`。

### 阶段 2：多模型对比内存版

目标：

- 可选择两个模型。
- 同一问题并发生成。
- 并排显示流式回答。
- 支持单独停止、全部停止、单独重试。
- 支持部分成功。

不做：

- 持久化。
- diff。
- 自动评分。

新增：

- `src/composables/useModelComparison.ts`
- `src/views/CompareChat.vue`
- 对比相关组件和样式。

验收：

- 两个 run 能同时 stream。
- A 成功 B 失败时，A 仍然显示 done。
- B 可单独重试。
- 停止 A 不影响 B。
- 停止全部能中断所有进行中的 run。

进展记录：

- 已完成对比类型：`ComparisonRunStatus`、`ComparisonRun`、`ComparisonSession`。
- 已新增 `src/composables/useModelComparison.ts` 内存核心。
- 已支持同一 prompt 下多个 runtime 并发调用 `generateStreamWithContext`。
- 已支持每个 run 独立 `AbortController`、状态、内容、错误、开始/结束时间、耗时。
- 已支持 `startComparison()`、`stopRun()`、`stopAll()`、`retryRun()`。
- 已新增 `src/views/CompareChat.vue`，接入最小对比页面。
- 已新增 `src/components/ComparisonRunCard.vue`，展示单个 run 的模型、状态、耗时、错误和内容。
- 已在 `src/App.vue` 增加“普通聊天 / 多模型对比”模式切换，普通聊天仍保留原链路。
- 当前 UI 先支持两个 runtime 槽位，从当前设置生成快照并允许手动编辑 provider/model/baseUrl/apiKey。
- 尚未接 DB，尚未做汇总模型。
- 已验证 `npm run lint` 通过。
- 已验证 `npm run build` 通过。

### 阶段 3：汇总模型

目标：

- 可选择一个模型作为汇总模型。
- 汇总模型接收成功 run 的回答。
- 生成最终 Markdown 答案。

验收：

- 至少一个 run 成功后可汇总。
- 汇总失败不影响原始结果。
- 汇总结果显示 source run ids 或模型名。
- 汇总前明确展示目标汇总模型。

进展记录：

- 已在 `src/composables/useModelComparison.ts` 增加 `summaryRun`、`isSummaryRunning`、`summarizeWith(runtime)`、`stopSummary()`。
- 已实现汇总 prompt 构造，汇总只使用 `done` 且有内容的成功 run。
- 已记录 summary run 的 `sourceRunIds`。
- 已在 `src/views/CompareChat.vue` 增加汇总模型选择、生成汇总、停止汇总和汇总结果展示。
- 已在汇总区域增加用户个性化汇总要求输入，并将该要求写入汇总 prompt。
- 已验证 `npm run lint` 通过。
- 已验证 `npm run build` 通过。

### 阶段 4：持久化

目标：

- 保存 comparison session/run。
- 页面刷新后可重新打开已完成记录。
- 可删除对比记录。

改动：

- Dexie version 4。
- 新增 comparison tables。
- 新增 comparison repository/service。

验收：

- 完成后的对比记录刷新后仍可查看。
- run 状态、耗时、错误、模型配置快照可恢复。
- 普通聊天历史不受影响。

进展记录：

- 已将 Dexie 升级到 version 4，新增 `comparisonSessions` / `comparisonRuns` 两张独立表。
- 已新增 `src/services/comparisonPersistence.ts`，负责保存、读取、列出、删除对比记录。
- 已在保存 runtime 快照前清理 `apiKey`，避免把 API key 写入 IndexedDB 历史记录。
- 已在 `src/composables/useModelComparison.ts` 接入持久化：
  - 创建对比 session 时先落库。
  - 每个 run 完成、失败或停止后保存最终状态。
  - 汇总 run 创建和完成后保存，并记录 `sourceRunIds`。
  - 页面恢复时将残留的 `loading/streaming` 状态归一为 `aborted`。
- 已在 `src/views/CompareChat.vue` 增加历史记录入口，支持打开、刷新和删除本地对比记录。
- 已保持普通聊天 `conversations/messages` 表和搜索索引逻辑不变。
- 已验证 `npm run lint` 通过。
- 已验证 `npm run build` 通过。

### 阶段 5：增强体验

目标：

- 搜索或筛选对比记录。
- 导出 Markdown/JSON。
- 回答长度、耗时、失败率统计。
- 文本差异高亮。
- 代码回答支持 diff 视图。

注意：

- 普通自然语言不适合直接做 Git 风格行级冲突处理。
- 代码类回答可以后续考虑 Monaco Diff Editor 或轻量 diff 库。

进展记录：

- 已新增 `src/services/comparisonExport.ts`，支持把当前对比记录导出为 Markdown / JSON。
- 导出 JSON 时会清理 runtime 快照中的 `apiKey`，避免把密钥写入导出文件。
- 已在 `src/views/CompareChat.vue` 增加“导出 MD / 导出 JSON”入口。
- 已增加历史查看模式提示，说明恢复记录不包含 API Key，适合查看和导出。
- 历史查看模式下禁用重试和重新汇总，避免误用已恢复的旧 runtime。
- 已新增 `src/services/comparisonStats.ts`，统一计算 run 字数、词数、session 状态分布和平均耗时。
- 已在单个模型回答卡片展示字符数、词数和耗时。
- 已在对比页顶部展示当前 session 的成功/失败/停止/平均耗时概览。
- 已在历史列表展示每条记录的成功/失败/停止数量。

## 10. 首版验收标准

首版完成标准：

- 普通聊天功能不回退。
- 多模型对比最多默认 2 个模型。
- 两个模型能并发流式回答。
- 每个模型 run 独立状态、独立停止、独立重试。
- 一个模型失败不影响另一个模型。
- 至少一个模型成功后可发起汇总。
- 汇总模型只接收成功结果。
- 汇总失败不影响原始对比结果。
- 每个 run 展示 provider/model/status/latency/error。
- 请求过程中修改全局设置不影响已启动 run。
- 图片输入时不支持视觉的模型不能直接参与对比。
- `npm run lint` 通过。
- `npm run build` 通过。

## 11. 后续暂缓事项

这些能力有价值，但不进入首版：

- 真正多 Agent 编排。
- Agent 工具调用。
- 自动任务拆解。
- Web 搜索。
- 本地文件系统读写。
- 代码执行。
- 自动裁判模型胜负。
- Git 冲突式片段合并。
- 长期记忆。
